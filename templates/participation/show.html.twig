{% extends 'base.html.twig' %}

{% block title %}Participation{% endblock %}

{% block body %}
    <h1>Participation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Participate</th>
                <td>{{ participation.participate ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Date participate</th>
                <td>{{ participation.dateParticipate ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('participation_index') }}">back to list</a>

    <a href="{{ path('participation_edit', {'id': participation.id}) }}">edit</a>

    {{ include('participation/_delete_form.html.twig') }}
{% endblock %}
