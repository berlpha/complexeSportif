{% extends 'base.html.twig' %}

{% block title %}Nursery{% endblock %}

{% block body %}
    <h1>Nursery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name of child</th>
                <td>{{ nursery.nameChild }}</td>
            </tr>
            <tr>
                <th>First name</th>
                <td>{{ nursery.firstName }}</td>
            </tr>
            <tr>
                <th>Date of custody</th>
                <td>{{ nursery.dateCustody }}</td>
            </tr>
            <tr>
                <th>TotalPrice</th>
                <td>{{ nursery.totalPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nursery_index') }}">back to list</a>

    <a href="{{ path('nursery_edit', {'id': nursery.id}) }}">edit</a>

    {{ include('nursery/_delete_form.html.twig') }}
{% endblock %}
