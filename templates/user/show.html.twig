{% extends 'admin.html.twig' %}

{% block title %}{% trans %}User{% endtrans %}{% endblock %}

{% block body %}

    <div class="container navbar-fixed-top"  style="position: relative; top: 31em">

        {{ include('Errors/errors.html.twig') }}

        <h3 style="text-align: center; color: red; background-color: black">{% trans %}User{% endtrans %}</h3><br><br>

            <table class="table table-striped table-sm">
                <tbody>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}Name{% endtrans %}</th>
                        <td>{{ user.name }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}FirstName{% endtrans %}</th>
                        <td>{{ user.firstName }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}Birthdate{% endtrans %}</th>
                        <td>{{ user.birthdate ? user.birthdate|date('Y-m-d') : '' }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}Username{% endtrans %}</th>
                        <td>{{ user.username }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}EmailAddress{% endtrans %}</th>
                        <td>{{ user.emailAddress }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}Password{% endtrans %}</th>
                        <td>{{ user.password }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}Phone{% endtrans %}</th>
                        <td>{{ user.phone }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}Address{% endtrans %}</th>
                        <td>{{ user.address }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}Number{% endtrans %}</th>
                        <td>{{ user.number }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}PostalCode{% endtrans %}</th>
                        <td>{{ user.postalCode }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}City{% endtrans %}</th>
                        <td>{{ user.city }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}Country{% endtrans %}</th>
                        <td>{{ user.country }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}Avatar{% endtrans %}</th>
                        <td>{{ user.avatar }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}Roles{% endtrans %}</th>
                        <td>{{ user.roles ? user.roles|join(', ') : '' }}</td>
                    </tr>
                    <tr>
                        <th style="color: blue; font-size: 13px">{% trans %}CreatedAt{% endtrans %}</th>
                        <td>{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                </tbody>
            </table>

        <a href="{{ path('user_index') }}"><button class="btn btn-primary btn-sm" style="background-color: blueviolet"><span class="glyphicon glyphicon-home"></span> {% trans %}Back to list{% endtrans %}</button></a>

        <a href="{{ path('user_edit', {'id': user.id}) }}"><button class="btn btn-primary btn-sm" style="background-color: yellowgreen"><span class="glyphicon glyphicon-pencil"></span> {% trans %}Edit{% endtrans %}</button></a><br><br>

        {{ include('user/_delete_form.html.twig') }}<br><br><br><br>

    </div>

{% endblock %}
