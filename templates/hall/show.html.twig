{% extends 'admin.html.twig' %}

{% block title %}Hall{% endblock %}

{% block body %}

    <div class="container navbar-fixed-top"  style="position: relative; top: 10em; margin-top: auto">

        <h3 style="text-align: center; color: red; background-color: black"><em>Salle</em></h3><br><br>

        <table class="table table-striped table-sm">
            <tbody>
                <tr>
                    <th style="color: blue">Name</th>
                    <td>{{ hall.name }}</td>
                </tr>
                <tr>
                    <th style="color: blue">Capacity</th>
                    <td>{{ hall.capacity }}</td>
                </tr>
            <tr>
                <th style="color: blue">Price Hour</th>
                <td>{{ hall.priceHour }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('hall_index') }}"><buttom class="btn btn-primary btn-sm" style="background-color: blueviolet">Retour</buttom></a>

        <a href="{{ path('hall_edit', {'id': hall.id}) }}"><buttom class="btn btn-primary btn-sm" style="background-color: yellowgreen">Éditer</buttom></a><br><br>

        {{ include('hall/_delete_form.html.twig') }}<br><br>

    </div><br><br>

{% endblock %}
