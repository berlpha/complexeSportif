{% extends 'admin.html.twig' %}

{% block title %}Field{% endblock %}

{% block body %}

    <div class="container navbar-fixed-top"  style="position: relative; top: 8em">

        <h3 style="text-align: center; color: red; background-color: black"><em>Terrain</em></h3><br><br>

            <table class="table table-striped table-sm">
                <tbody>
                <tr>
                    <th style="color: blue">Name</th>
                    <td>{{ field.name }}</td>
                </tr>
                <tr>
                    <th style="color: blue">Capacity</th>
                    <td>{{ field.capacity }}</td>
                </tr>
                <tr>
                    <th style="color: blue">Price Hour</th>
                    <td>{{ field.priceHour }}</td>
                </tr>
                </tbody>
            </table>

        <a href="{{ path('field_index') }}"><button class="btn btn-primary btn-sm" style="background-color: blueviolet">Retour</button></a>

        <a href="{{ path('field_edit', {'id': field.id}) }}"><button class="btn btn-primary btn-sm" style="background-color: yellowgreen">Éditer</button></a><br><br>

        {{ include('field/_delete_form.html.twig') }}<br><br>

    </div>

{% endblock %}
